import './App.css';
import Form from './components/Form';

function App() {
  return (
    <>
      <h2>MyNote</h2>
      <p>If you are enter a value in input than click addnote button so, it's create new note</p>
      <p>If you are enter a value in input than click edit button so, it's edit note</p>
      <Form />
    </>
  );
};

export default App;
